models:
  test:
    compress_with:
      type: tgz
    storages:
      local:
        type: local
        keep: 10
        path: tests/backups/
    archive:
      includes:
        - tests/fixtures/files/
